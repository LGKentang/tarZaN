using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PathWalker : MonoBehaviour
{
    //public PathBuilder pb;

    void Start()
    {
        //pb = GetComponent<PathBuilder>();
        //pb.pf.FindPath(pb.grid.GetSpecifiedStartingPoint(NodeType.first),pb.grid.targetNode, true);
    }


}
